const config = {
	fontFamily: ["Public Sans", "sans-serif"].join(","),
	i18n: "en",
	miniDrawer: false,
	container: true,
	mode: "light",
	presetColor: "default",
	themeDirection: "ltr",
};

export default config;
export const drawerWidth = 260;
export const ITEM_HEIGHT = 48;

export const buttonSelectedColor = "primary.100";
export const buttonHovered = "primary.200";
