import { MyTableHead } from "@myTypes/table";

type TableHeadBuilder = {
	headers: MyTableHead[];
};
const TableHeadBuilder = (props: TableHeadBuilder) => {
	return <div>Enter</div>;
};

export default TableHeadBuilder;
